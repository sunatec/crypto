# 任务：结构化波浪报告 + 平票口径

> 仅改 `analyze-wave-tree.js`。分步见 design §4。

## 1. 平票口径（先做，引擎级）✅

- [x] 1.1 新增 `GRAMMAR_OVERSHOOT=0.25`，`decompose` 里 `tier2 += grammarViolations*GRAMMAR_OVERSHOOT`。
      显示与排名解耦：grammarNotes.overshoot 仍记 0（违规原因照旧叙述为结构性、不显示"差X%"），
      只有排序用的 tier2 带权重。
- [x] 1.2 金标准回归：主选由"结构不对的单锯齿(文法违规)"变为 **W-X-Y 双锯齿
      126296→80524→97964→57718**（tier1=1、tier2=0.005，仅差 y/w 比例 0.5%）；单锯齿 tier2 被
      权重压到 0.25、退到末位。正是探索 D 要的"主选对齐真实拟合"。
- [x] 1.3 全套测试 90/90 全绿（新增金标准断言锁定"主选=W-X-Y、单锯齿被压后"）。

## 2. 新增计算 ✅

- [x] 2.1 关键转折点"暂定浪型位置"映射：遍历主方案 points，把每个转折点标到第几浪
- [x] 2.2 W/X/Y（及所选结构）各腿的时间比例（腿时长之比）
- [x] 2.3 "为何不是单锯齿/五浪/平台"对比叙述：复用各竞争候选的 violations，生成排除理由
- [x] 2.4 wave-3 结构验证清单（书内、去动能）：第1浪存在/第2浪合规/未破第1浪起点/破第1浪高点/
      内部五浪/破前一轮关键结构高点；数据不足则标"无法验证"
- [x] 2.5 高/中/低分档函数（按 违规数+越界+未完成度）

## 3. 结构化报告渲染（7 节）✅

- [x] 3.1 §1 分析范围（复用 renderReportMeta，改标题）
- [x] 3.2 §2 关键转折点表（+ 暂定浪型位置列）
- [x] 3.3 §3 已完成浪型（逐腿起止/内部/价格比例/时间比例/排除理由）
- [x] 3.4 §4 当前主方案（当前浪型/修正对象/计算基准/比例规则/目标价/确认条件/失效条件）
- [x] 3.5 §5 备选方案（≥2，各带修正对象+目标）
- [x] 3.6 §6 方案排序表（高中低档/支持依据/不利因素/确认条件）
- [x] 3.7 §7 最终话术（模板化拼接前几节要素成连续自然语言，数字与前文一致、不臆造）
- [x] 3.8 CLI 开关（如 `--report structured` 或独立输出文件），现有报告保留

## 4. 验证 ✅

- [x] 4.1 反弹方案强制点明修正对象（XX→上一子浪、B→整段）——缺失即视为不合格
- [x] 4.2 3 浪方案：逐条结构验证渲染；不因涨幅大直接判 3 浪；无书外指标
- [x] 4.3 1d/1h 端到端：七节齐备、无 undefined/NaN、数字自洽、性能正常
- [x] 4.4 单测：浪位映射、时间比例、高中低分档、wave-3 清单、文法越界权重

## 实现说明
- 新增 `renderStructuredReport(meta,tree,candles,fine)` 一次产出 7 节；复用 renderReportMeta/
  buildRankedCounts/deriveParentDegree/bounceHypotheses/inProgressStruct/candidateViolations，
  只做组织与叙述、不臆造数字。main() 每次额外写一份 `*_structured.md`（现有 `*_tree.md` 保留）。
- §3"为何不是单锯齿/五浪/平台"：从「全部候选(tree.primary+alternates)」按 patternId 找该形态
  最优代表、引用其 violations（不只在去重后的 top 里找，保证三种都能给出理由）。
- §6 用「主选=高 / 次选=中 / 淘汰=低」的 rank 分档（父级候选经否定法闸门排序，rank 即强弱）；
  `strengthTier(score)` 是按 违规数+越界+未完成度 的分数版分档工具（已导出+单测），供数法系候选用。
- wave-3 清单只做书内结构 6 项（去动能）；实测当前 4/6 通过（内部三波、未破关键前高两项未过）→
  据实"暂不提升 3 浪档"，落地"不得因涨幅大直接判 3 浪"。
- 92/92 测试全绿（新增 strengthTier、结构化报告 7 节+修正对象+wave-3+不给概率 断言）；1d/1h 端到端
  七节齐备、无 undefined/NaN。
